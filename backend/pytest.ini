[pytest]
# services/ 에 __init__.py 가 없어 암묵적 네임스페이스 패키지로 동작한다.
# 이 경우 `pytest` 실행파일로 돌리면 backend/ 가 sys.path 에 들어가지 않아
# `from services.mwis...` 임포트가 실패한다(`python -m pytest` 는 cwd 를
# 넣어주므로 통과). pythonpath 를 명시해 두 방식 모두 동일하게 동작시킨다.
pythonpath = .
testpaths = services
